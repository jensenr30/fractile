#include "fractal.h"
#include <SDL2/SDL.h>

/// this function will render a fractal to a destination SDL_Surface
// frac is the fractal to render
// dest is the destination SDL_Surface to render the fractal to
// x and y are offsets from the origin of the fractal
// with x and y both set to 0, the fractal will render with the origin of the fractal in the middle of the destination surface.
// zoom specifies the scale to render everything at.
void fractal_render(struct fractal *frac, SDL_Surface *dest, float x, float y, float zoom)
{
	
}
